Connected Home Server for NSDL C

Zach Shelby <zach@sensinode.com>

This project is an example of a NanoService endpoint for the Connected Home Reference App, which emulates the sensors of 
a power measurement and relay node.  

Compilation:

This software supports both Linux and MAC OSX builds, controlled by the "OS =" line of Makefile. 
Set this line to "LINUX" for Linux OSs and to "MACOSX" for Macs. 

To build the software type:

make

or if build the endpoint with eDTLS library, type:

make EDTLS=YES

Usage:

connected-home [-p 8000] 
-p	port to listen on (default = 8000)
-d	NSP IPv6 address (default = ::1)
-dp NSP port number (default = 5683)

When running on the same machine as NSP, simply run:

./connected-home


Known Limitations:

- IPv4 not supported

